import { Dimensions, StyleSheet } from "react-native";
import palette from "../constants/palette";
const { width } = Dimensions.get("window");

const testingZone = () => {
  console.log("testingZone");
};

export default testingZone;
